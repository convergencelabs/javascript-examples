// 1. Rename this file to config.js and modify the below.

// 2. Modify the url to match your convergence username and the domainId of the
//    domain you wish to use.
var DOMAIN_URL = "https://api.convergence.io/realtime/domain/<convergence-username>/<domainId>";

// 3. Modify the username and password to match a domain user that exists within
//    your domain.  Such a user can be created in the Convergence Admin Console.
var DOMAIN_USERNAME = "<domain-username>";
var DOMAIN_PASSWORD = "<password>";